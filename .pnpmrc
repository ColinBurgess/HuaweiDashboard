# pnpm configuration
# Allow build scripts from dependencies
shamefully-hoist=false
strict-peer-dependencies=false

# Security policies
auto-install-peers=true

# Build-only dependencies (do not hoist to node_modules)
only-built-dependencies=@google/genai,esbuild,protobufjs
